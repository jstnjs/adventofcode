import { challenge01, challenge02 } from './day-05';

describe('day05', () => {
  it('should work', () => {
    expect(challenge01()).toEqual('day-05');
  });
  it('should work', () => {
    expect(challenge02()).toEqual('day-05');
  });

});
