import { challenge01, challenge02 } from './day-03';

describe('day03', () => {
  it('should work', () => {
    expect(challenge01()).toEqual(7701);
  });

  it('should work 02', () => {
    expect(challenge02()).toEqual(2644);
  });
});
