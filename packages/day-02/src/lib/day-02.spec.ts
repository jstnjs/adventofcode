import { challenge01, challenge02 } from './day-02';

describe('day02', () => {
  it('should work', () => {
    expect(challenge01()).toEqual(13052);
  });

  it('should work 02', () => {
    expect(challenge02()).toEqual(13693);
  });

});
