import { challenge01, challenge02 } from './day-04';

describe('day04', () => {
  it('should work', () => {
    expect(challenge01()).toEqual(532);
  });

  it('should work 02', () => {
    expect(challenge02()).toEqual(854);
  });
});
