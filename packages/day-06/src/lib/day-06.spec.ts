import { challenge } from './day-06';

describe('day06', () => {
  it('should work', () => {
    expect(challenge(4)).toEqual(1300);
  });

  it('should work', () => {
    expect(challenge(14)).toEqual(3986);
  });
 
});
